"""Pipeline V2 — multi-stage Kaizer News pipeline.

Public re-exports kept minimal; consumers should import from the
sub-modules directly (e.g. ``from pipeline_v2.models import JobOutput``).
"""

__version__ = "0.1.0"
