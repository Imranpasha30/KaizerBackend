[phases.setup]
nixPkgs = ["ffmpeg"]

[phases.build]
cmds = ["python manage.py migrate --noinput", "python manage.py collectstatic --noinput"]

[start]
cmd = "gunicorn kaizer_news.wsgi --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
